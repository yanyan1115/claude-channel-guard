# Guarded Telegram Plugin Template

Copy this directory to a local Claude Code plugin path and edit `.mcp.json`.

The plugin name must remain `telegram` so Claude Code loads the channel under the same tool namespace expected by the official Telegram channel.

Do not copy your Telegram `.env`, access file, token, or chat IDs into this repository.
